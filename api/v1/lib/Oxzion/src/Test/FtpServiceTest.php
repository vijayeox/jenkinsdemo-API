<?php
namespace MessageTest;

use Mockery;
use PHPUnit\Framework\TestCase;
use Oxzion\Service\FtpService;

class FtpServiceTest extends TestCase
{
    
    public function getConnection(){
        // $ftpService = FtpServiceTest::getInstance($host, $user, $password, true, $port);
        // $ftpService->uploadFile($source_file_path, $target_file_path);
        // $ftpService->downLoad($source_directory, $pattern, $target_directory, FTP_BINARY);
    }
}
